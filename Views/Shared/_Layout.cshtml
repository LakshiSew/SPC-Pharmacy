<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Sidebar */
        .sidebar {
            background-color: #343a40;
            color: white;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            box-shadow: 2px 0px 5px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding-top: 10px;
        }

        .sidebar h2 {
            text-align: center;
            color: #fff;
            font-size: 22px;
            padding: 10px;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .sidebar ul li {
            padding: 8px 12px;
            border-radius: 4px;
            transition: background-color 0.3s ease;
            margin-bottom: 5px; /* Reduced gap */
        }

        .sidebar ul li.active-link {
            background-color: #007bff;
        }

        .sidebar ul li:hover {
            background-color: #007bff;
        }

        .sidebar ul li a {
            text-decoration: none;
            color: white;
            font-size: 16px;
            display: flex;
            align-items: center;
            padding: 8px 10px;
            width: 100%;
        }

        .sidebar ul li a i {
            margin-right: 8px; /* Reduced spacing between icon and text */
            font-size: 18px;
        }

        /* Logout Button */
        .logout-btn {
            width: 90%;
            padding: 10px;
            background-color: #dc3545;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-align: center;
            font-size: 16px;
            margin: 15px auto;
            display: block;
        }

        .logout-btn i {
            margin-right: 8px;
        }

        .logout-btn:hover {
            background-color: #c82333;
        }

        /* Main Content */
        .main-content {
            margin-left: 260px;
            padding: 20px;
            transition: margin-left 0.3s ease;
        }

        .container-fluid {
            padding-left: 0;
        }

        /* Responsive Design */
        media (max-width: 768px) {
            .sidebar {
                width: 200px;
            }

            .main-content {
                margin-left: 210px;
            }
        }

        media (max-width: 576px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }

            .main-content {
                margin-left: 0;
            }

            .sidebar h2 {
                font-size: 18px;
            }

            .sidebar ul li {
                font-size: 16px;
            }

            .logout-btn {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar Section -->
            <div class="col-md-3 sidebar">
                <h2>Admin Panel</h2>
                <ul>
                    <li><a href="@Url.Action("AdminDashboard", "Home")" class="nav-link"><i class="fas fa-home"></i> Home</a></li>
                    <li><a href="@Url.Action("GetAdmin", "Home")" class="nav-link"><i class="fas fa-user"></i> Manage Admin</a></li>
                    <li><a href="@Url.Action("GetDrugs", "Home")" class="nav-link"><i class="fas fa-box"></i> Manage Drugs</a></li>
                    <li><a href="@Url.Action("GetStocks", "Home")" class="nav-link"><i class="fas fa-clipboard-list"></i> Stock Inventory</a></li>
                    <li><a href="@Url.Action("GetTenders", "Home")" class="nav-link"><i class="fas fa-file-contract"></i> Manage Tender</a></li>
                    <li><a href="@Url.Action("GetTenserProposals", "Home")" class="nav-link"><i class="fas fa-file-signature"></i> Tender Proposal</a></li>
                    <li><a href="@Url.Action("GetPharmacy", "Home")" class="nav-link"><i class="fas fa-clinic-medical"></i> Manage Pharmacy</a></li>
                </ul>
                <button class="logout-btn" onclick="location.href='@Url.Action("Logout", "Home")'">
                    <i class="fas fa-sign-out-alt"></i> Logout
                </button>
            </div>
            <!-- Main Content Section -->
            <div class="col-md-9 main-content">
                @RenderBody() <!-- This will render the content of the current view -->
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Highlight active page in sidebar
        $(document).ready(function () {
            var currentUrl = window.location.href;
            $('.nav-link').each(function () {
                if (this.href === currentUrl) {
                    $(this).closest('li').addClass('active-link');
                }
            });
        });
    </script>
</body>
</html>
